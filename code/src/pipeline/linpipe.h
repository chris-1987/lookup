#ifndef _LINPIPE_H
#define _LINPIPE_H


/// \brief Linear pipeline. 
/// 
/// A linear pipeline stores a prefix tree in a manner of one level per stage.
/// A search request starts from the initial stage and steps through the following stages to traverse down the prefix tree from the root node to a leaf node.


#endif
